document.addEventListener('DOMContentLoaded', async () => {
    
    // call the total global value in the other JS file, this is most definitely bad practice
    // I should probs be storing it locally with chrome.storage.set and getting it with 
    // chrome.storage.get, but this should work for the MVP
    // document.getElementById("display-count").innerText = total;

    

});